# LLM 配置（OpenAI 兼容 API），复制为 .env 并填入真实值
# 放置位置：dev 放项目根目录（Vite 启动时注入）；打包的 release exe 放 exe 同目录
# （运行时读取，构建期不需要 .env）。修改后需重启应用生效。
# 填服务商文档给的完整 baseURL（一般已含 /v1），代码原样透传、不做任何加工
VITE_LLM_API_URL=https://api.openai.com/v1
VITE_LLM_API_KEY=sk-xxx
VITE_LLM_MODEL=gpt-4o-mini
